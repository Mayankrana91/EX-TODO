<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>🌿 ExTodo - <%= username %></title>
    <!-- Using the same modern font as the header and footer -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --brand-dark: #2c3e50;
            --accent-color: #e74c3c;
            --background-color: #f5f7fa;
            --card-bg: #ffffff;
            --text-dark: #333333;
            --text-light: #6b7280;
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }

        * { 
            box-sizing: border-box; 
            margin: 0; 
            padding: 0; 
        }

        body {
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background: var(--background-color);
            color: var(--text-dark);
            padding-top: 80px; /* Space for the fixed navbar */
            padding-bottom: 60px; /* Space for the fixed footer */
        }
        
        main {
            flex: 1;
            padding: 2rem 1rem;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .welcome-section {
            text-align: center;
            margin-bottom: 2.5rem;
            max-width: 600px;
        }

        .welcome-section h1 {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--brand-dark);
            margin-bottom: 0.5rem;
        }
        
        .welcome-section p {
            font-size: 1rem;
            color: var(--text-light);
            margin-bottom: 1rem;
        }
        
        /* Add Todo Form */
        .add-form {
            display: flex;
            width: 100%;
            max-width: 500px;
            margin-bottom: 2rem;
            box-shadow: var(--shadow-md);
            border-radius: 999px;
            overflow: hidden;
            background: var(--card-bg);
            transition: box-shadow 0.3s ease;
        }

        .add-form:focus-within {
            box-shadow: var(--shadow-lg);
        }

        .add-form input {
            flex: 1;
            padding: 0.8rem 1.5rem;
            border: none;
            font-size: 1rem;
            outline: none;
            background: transparent;
        }

        .add-form button {
            padding: 0.8rem 1.5rem;
            border: none;
            background: var(--accent-color);
            color: var(--card-bg);
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .add-form button:hover {
            background: #c0392b; /* a darker shade of the accent color */
        }

        /* Todo Cards */
        .todo-list-container {
            width: 100%;
            max-width: 500px;
        }

        .todo-card {
            background: var(--card-bg);
            width: 100%;
            padding: 1rem 1.5rem;
            border-radius: 12px;
            margin-bottom: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: var(--shadow-md);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        
        .todo-card:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-lg);
        }

        .todo-card.completed {
            opacity: 0.6;
            text-decoration: line-through;
            background-color: #f0f4f7;
        }
        
        .todo-card span:first-child {
            flex: 1;
            margin-right: 1rem;
            word-break: break-word;
        }
        
        .todo-card .button-group {
            display: flex;
            gap: 0.5rem;
        }
        
        .todo-card button {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 500;
            transition: background-color 0.3s ease;
        }

        .done-btn {
            background: #34d399; /* emerald-500 */
            color: #fff;
        }
        .done-btn:hover {
            background: #10b981; /* emerald-600 */
        }
        .delete-btn {
            background: #ef4444; /* red-500 */
            color: #fff;
        }
        .delete-btn:hover {
            background: #dc2626; /* red-600 */
        }

        @media(max-width:600px){
            .welcome-section h1 {
                font-size: 2rem;
            }
            .welcome-section p {
                font-size: 0.9rem;
            }
            .add-form { 
                flex-direction: column; 
                border-radius: 12px; 
            }
            .add-form input, 
            .add-form button { 
                width: 100%; 
                border-radius: 8px;
                margin-bottom: 0.5rem;
            }
        }
    </style>
</head>
<body>

    <%- include('navbar', { username: username }) %>

    <main>
        <section class="welcome-section">
            <h1>Welcome, <%= username === "guest" ? "Guest" : username %>!</h1>
            <p><%= username === "guest" ? "Login to save and manage your todos." : "Your Todo Dashboard:" %></p>
        </section>

        <% if(username !== "guest") { %>
            <form class="add-form" action="/home/add" method="POST" onsubmit="saveToLocalStorage()">
                <input type="hidden" name="username" value="<%= username %>">
                <input type="text" id="task" name="task" placeholder="Add a new task..." required>
                <button type="submit">Add</button>

            </form>


            <script>
            function saveToLocalStorage() {
            let task = document.getElementById("task").value.trim();
                if (!task) return;

            let todos = JSON.parse(localStorage.getItem("todos")) || [];
            todos.push({ id: Date.now(), text: task, completed: false });
            localStorage.setItem("todos", JSON.stringify(todos));
          }
            </script>






            <div class="todo-list-container">
                <% if(todos.length === 0) { %>
                    <p>No todos yet! Add one above to get started. </p>
                <% } else { %>
                    <% todos.forEach(todo => { %>
                        <div class="todo-card <%= todo.completed ? 'completed' : '' %>">
                            <span><%= todo.text %></span>
                            <div class="button-group">
                                <form style="display:inline" action="/home/toggle" method="POST">
                                    <input type="hidden" name="taskId" value="<%= todo.id %>">
                                    <input type="hidden" name="username" value="<%= username %>">
                                    <button type="submit" class="done-btn"><%= todo.completed ? 'Undo' : 'Done' %></button>
                                </form>
                                <form style="display:inline" action="/home/delete" method="POST">
                                    <input type="hidden" name="taskId" value="<%= todo.id %>">
                                    <input type="hidden" name="username" value="<%= username %>">
                                    <button type="submit" class="delete-btn">Delete</button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                <% } %>
            </div>
        <% } %>
    </main>


    <%- include('footer') %>
</body>
</html>
