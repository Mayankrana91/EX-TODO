<style>
  .navbar {
    height: 60px;
    background: #fff;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 1rem;
    border-bottom: 1px solid #ddd;
  }
  .navbar .logo { font-weight: 700; font-size: 1.2rem; color: #2c3e50; }
  .navbar input {
    padding: 0.4rem 0.8rem; border-radius: 6px; border: 1px solid #ddd; width: 250px;
  }
  .navbar .nav-right { display: flex; gap: 1rem; align-items: center; }
  .navbar a { text-decoration: none; font-weight: 600; color: #2c3e50; }
  .navbar a:hover { color: #e74c3c; }
</style>

<div class="navbar">
  <div class="logo">🌿 ExTodo</div>
  <div class="nav-right">
    <% if(username !== "guest") { %>
      <span>Hello, <%= username %></span>
      <a href="/auth/logout">Logout</a>
    <% } else { %>
      <a href="/auth/login">Login</a>
      <a href="/auth/signup">Signup</a>

    <% } %>
  </div>
</div>
