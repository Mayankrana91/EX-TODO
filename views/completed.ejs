<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>🌿 ExTodo - Completed Tasks</title>

  <style>
    :root {
      --brand-dark: #2c3e50;
      --accent: #f39c12;
      --bg: #f9f9f9;
      --card: #fff;
      --text: #333;
      --muted: #888;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Poppins', sans-serif; }
    body { background: var(--bg); color: var(--text); display: flex; flex-direction: column; height: 100vh; }
    .layout { flex: 1; display: flex; }

    /* Main Content */
    .content { flex: 1; padding: 2rem 1.5rem; overflow-y: auto; }
    .content h1 {
      font-size: 1.6rem; font-weight: 700; color: var(--brand-dark);
      margin-bottom: 1.5rem;
    }

    /* Completed Todos List */
    .todo-list-container { max-width: 700px; margin: auto; display: flex; flex-direction: column; gap: 1rem; }
    .todo-card {
      display: flex; flex-direction: column; gap: 0.4rem;
      background: var(--card); padding: 1rem 1.2rem; border-radius: 10px;
      border: 1px solid #eee;
      transition: box-shadow 0.2s ease;
      /* text-decoration: line-through;
      opacity: 0.7; */
    }
    .todo-card:hover { box-shadow: 0 4px 8px rgba(0,0,0,0.08); }

    .todo-top { display: flex; justify-content: space-between; align-items: center; }
    .todo-text { font-weight: 500; flex: 1; }
    .todo-meta { font-size: 0.85rem; color: var(--muted); }

    .restore-btn {
      background: var(--accent); color: #fff;
      border: none; border-radius: 6px;
      padding: 0.4rem 0.8rem;
      cursor: pointer; font-weight: 600;
      transition: background 0.2s ease;
    }
    .restore-btn:hover { background: #d98208; }

    .empty-message { text-align: center; font-size: 1.1rem; color: var(--muted); margin-top: 2rem; }

    /* Dark Mode */
    body.dark-mode { background: #121212; color: #f5f5f5; }
    body.dark-mode .todo-card { background: #555454; border: 1px solid #444; }
    body.dark-mode h1 { color: #f5f5f5; }
    body.dark-mode .todo-meta { color: #aaa; }
  </style>
</head>
<body>
  <!-- Navbar -->
  <%- include("navbar", { username: username }) %>

  <!-- Layout -->
  <div class="layout">
    <% if(username !== "guest") { %>
      <!-- Sidebar -->
      <%- include("sidebar") %>
    <% } %>

    <!-- Content -->
    <main class="content">
      <h1>Completed Tasks</h1>
      <div class="todo-list-container">
        <% if (completedTodos.length === 0) { %>
          <p class="empty-message">No completed tasks yet ✅</p>
        <% } else { %>
          <% completedTodos.forEach(todo => { %>
            <div class="todo-card">
              <div class="todo-top">
                <span class="todo-text"><%= todo.text %></span>
                <form action="/completed/restore" method="POST">
                  <input type="hidden" name="taskId" value="<%= todo.id %>">
                  <input type="hidden" name="username" value="<%= username %>">
                  <button type="submit" class="restore-btn">↩ Restore</button>
                </form>
              </div>
<span class="todo-meta">
  <% if (todo.completedAt) { %>
    Completed on <%= new Date(todo.completedAt).toLocaleDateString() %> at
    <%= new Date(todo.completedAt).toLocaleTimeString([], {hour:'2-digit', minute:'2-digit'}) %>
  <% } else { %>
    Completed (no timestamp available)
  <% } %>
</span>

            </div>
          <% }) %>
        <% } %>
      </div>
    </main>
  </div>

  <!-- Footer -->
  <%- include("footer") %>
</body>
</html>
